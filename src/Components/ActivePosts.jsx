import React from "react";

const ActivePosts = ({ className = "" }) => {
    return (<div className={`${className} bg-superClear rounded-md`}>
        Item 5
    </div>);
}

export default ActivePosts;